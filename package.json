{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "clean": "lerna run clean --parallel",
    "dev": "lerna run dev --parallel",
    "build": "lerna run build --parallel"
  }
}
